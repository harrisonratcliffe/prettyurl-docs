# API Limitations

At the moment, all API requests are restricted to **50 requests per second (RPS)**. In the future, we will be introducing more limitations for different types of endpoints as well as package-specific limits.
